# Bundled 7-Zip

本目录包含随 AndroidStudioWinHelper 分发的 7-Zip 命令行工具（`7z.exe` + `7z.dll`），
通过 `pubspec.yaml` 打包进 `data/flutter_assets/tools/7zip/`，**不会**再单独安装到 `{app}\tools\7zip`。

- 许可：GNU LGPL（见 `License.txt`）
- 官网：https://www.7-zip.org/
